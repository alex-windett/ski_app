<p id="notice"><%= notice %></p>

<h2> <%= @run.name %> </h2>

<p><%= @run.description %></p>

<p><%= @run.resort %></p>

<p><%= @run.rating %></p>

<!-- <table class='run_info'>
    <thead>
      <tr>
        <th>ID</th>
        <th>Latitude</th>
        <th>Longitude</th>
        <th>Elevation</th>
        <th>Time</th>
      </tr>
    </thead>
    <tbody>
      <% @run.markers.each do |marker| %>
        <tr>
          <td><%= marker.id %></td>
          <td><%= marker.latitude %></td>
          <td><%= marker.longitude %></td>
          <td><%= marker.elevation %></td>
          <td><%= marker.marker_created_at %></td>
        </tr>
      <% end %>
    </tbody>
</table> -->

<div class="geo_data">
  <p><button onclick="geoFindMe()">Start Tracking</button></p>
  <div id="output"></div>
</div>

<section>
  <div id="map_canvas"></div>
  <div class="graph"></div>
</section>


<%= link_to 'Edit', edit_run_path(@run) %> |
<%= link_to 'Back', runs_path %>